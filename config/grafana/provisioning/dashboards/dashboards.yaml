apiVersion: 1

providers:
  - name: telemetry
    orgId: 1
    folder: Telemetry
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: ${GF_DASHBOARDS_PATH}
      foldersFromFilesStructure: true
